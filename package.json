{
  "name": "trustedfare",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute trustedfare-bookings --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
