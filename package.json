{
  "name": "trustedfare",
  "version": "1.0.0",
  "description": "TrustedFare flight search & booking platform powered by Ignav API",
  "main": "src/worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute trustedfare-bookings --file=./schema.sql"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^4"
  }
}
